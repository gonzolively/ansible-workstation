---

# Ubuntu
- name: Install Regolith i3 WM if GUI exists
  package:
    name: regolith-desktop
    state: present
  # This is mostly for testing as I want to ignore this install
  #when: has_gui != ""

# Fedora

# Arch
